{% extends 'base.html.twig' %}
{% block body %}
    <link rel="stylesheet" href="{{asset('bundles/mdwgestion/css/style_ajout.css')}}">
    <h1>Personne creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_personne_index') }}">Back to the list</a>
        </li>
    </ul>

{% endblock %}
