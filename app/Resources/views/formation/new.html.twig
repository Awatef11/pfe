{% extends 'base.html.twig' %}
<link rel="stylesheet" href="{{asset('bundles/mdwgestion/css/style_show.css')}}">

{% block body %}
    <h1>Formation creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_formation_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
